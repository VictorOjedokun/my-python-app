# Dockerized Flask App

A basic Python Flask web app, containerized using Docker.

#  Technologies Used
- Python
- Flask
- Docker

#  How to Run

1. Build the Docker image:
   docker build -t flask-docker-app .

2. Run the container:
   docker run -p 5000:5000 xahavi/my-python-app

3. Visit http://localhost:5000

4. Pull image from docker hub
   docker pull xahavi/my-python-app:latest
